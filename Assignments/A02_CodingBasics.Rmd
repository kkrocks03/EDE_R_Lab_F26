---
title: "Assignment 2: Coding Basics"
author: "Kassie Karnow"
output: 
  pdf_document:
    toc: true
geometry: margin=2.54cm
editor_options: 
  chunk_output_type: console
---

## OVERVIEW

This exercise accompanies the lessons/labs in Environmental Data Explorations on reproducibility and coding basics.

## Directions

1.  Rename this file `<FirstLast>_A02_CodingBasics.Rmd` (replacing `<FirstLast>` with your first and last name).
2.  Change "Student Name" on line 3 (above) with your name.
3.  Work through the steps, **creating code and output** that fulfill each instruction.
4.  Be sure to **answer the questions** in this assignment document.
5.  When you have completed the assignment, **Knit** the text and code into a single PDF file.
6.  After Knitting, submit the completed exercise (PDF file) to Canvas.

## Crafting Reports - Part 1

### Import packages and data, suppressing messages

Set the following R code chunk so that it runs when knit, but no messages, errors, or any output is shown. The code itself, however, should be displayed.

```{r project.setup}
#Import libraries
install.packages('tidyverse')
install.packages('knitr')

#need to install packages first before being able to pull them from the library

```

## Coding Basics - Part 2

1.  Generate a sequence of numbers from one to 55, increasing by fives. Assign this sequence a name.

2.  Compute the mean and median of this sequence.

3.  Ask R to determine whether the mean is greater than the median.

4.  Insert comments in your code to describe what you are doing.

```{r basics 1}
#1.  
seq1<-(seq(1, 55, by = 5))
#creating a sequence from 1 to 55 by fives


#2. 
mean1<-mean(seq1)
median1<-median(seq1)
#calculating the mean and median of the sequence created

#3. 
mean1 > median1
#if mean is greater than median, code will return TRUE

```

## Coding Basics - Part 3

5.  Create three vectors, each with four components, consisting of (a) student names, (b) test scores, and (c) whether they are on scholarship or not (TRUE or FALSE).

6.  Label each vector with a comment on what type of vector it is.

7.  Combine each of the vectors into a data frame. Assign the data frame an informative name.

8.  Label the columns of your data frame with informative titles.

```{r basics 2}
vector1<-c("Kassie", "Kenneth", "Connor", "Kayla") #character vector
vector2<-c(100,96,42,87) #numeric vector
vector3<-c(TRUE,TRUE, FALSE, TRUE) #logical vector
dataframe1 <- data.frame(vector1,vector2,vector3) #combining vectors into a single data frame
names(dataframe1) <- c("Name","Test Score","Scholarship"); View(dataframe1) #providing titles for each vector in the data frame in the order that they appear 

```

9.  QUESTION: How is this data frame different from a matrix?

> Answer: This data frame is different from a matrix because it is less complex than a matrix. Additionally, both are two-dimensional, but a dataframe can be a mixture of elements and matrices are composed of one element type.

10. Create a function with one input. In this function, use `if`...`else` to evaluate the value of the input: if it is greater than 50, print the word "Pass"; otherwise print the word "Fail".

11. Create a second function that does the exact same thing as the previous one but uses `ifelse()` instead if `if`...`else`.

12. Run both functions using the value 52.5 as the input

13. Run both functions using the **vector** of student test scores you created as the input. (Only one will work properly...)

```{r basics 3}
#10. Create a function using if...else
#if (x > 50) {print("Pass")} else {print("Fail")}

#11. Create a function using ifelse()
#ifelse(x > 50, "Pass", "Fail")

#12a. Run the first function with the value 52.5
if (52.5 > 50) {print("Pass")} else {print("Fail")}

#12b. Run the second function with the value 52.5
ifelse(52.5 > 50, "Pass", "Fail")

#13a. Run the first function with the vector of test scores
#if (vector2 > 50) {print("Pass")} else {print("Fail")}

#13b. Run the second function with the vector of test scores
ifelse(vector2 > 50, "Pass", "Fail")
```

14. QUESTION: Which option of `if`...`else` vs. `ifelse` worked? Why? (Hint: search the web for R vectorization)

> Answer: When using the vector of test scores, ifelse worked and if...else did not work. If...else provided the error that "the condition has length \> 1. When researching this topic, I found that if...else uses scalar control flow, whereas ifelse uses vectorized data processing. This means that if...else runs everything in one go and provides a single true or false answer, whereas ifelse runs through each element individually in a dataset and will provide true/false for each individual element.

## Crafting reports - Part 4

### Knit and submit.

Add a table of contents to your document and knit to a PDF. Submit your PDF to Canvas, but also be sure to commit and push your Rmd file used to create this knit document to GitHub. In the section below, add a link to your GitHub repository.

### Github link

> Answer: https://github.com/kkrocks03/EDE_R_Lab_F26/tree/main

**NOTE** Before knitting, you'll need to comment out the call to the function in Q13 that does not work. (A document can't knit if the code it contains causes an error!)
